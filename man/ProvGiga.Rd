% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ProvGiga.R
\docType{class}
\name{ProvGiga}
\alias{ProvGiga}
\alias{ProvGiga-class}
\alias{ProvGigaCSV-class}
\alias{show,ProvGiga-method}
\alias{import,ProvGigaCSV,ANY,ANY-method}
\title{Import ProvGiga slide-level data into a Bioconductor class object}
\usage{
ProvGiga(
  resource,
  level = c("slide_level", "tile_level"),
  is_url = TRUE,
  tumorType = NA_character_
)

\S4method{show}{ProvGiga}(object)

\S4method{import}{ProvGigaCSV,ANY,ANY}(con, format, text, ...)
}
\arguments{
\item{resource}{\code{character(1)} the file path or URL to the ProvGiga CSV file,
or a \code{TENxFile} object.}

\item{level}{\code{character(1)} specifying the level of ProvGiga data to import.
Must be one of \code{"slide_level"} or \code{"tile_level"}. If not provided, the
level is inferred from the file path or URL.}

\item{is_url}{\code{logical(1)} indicating whether the \code{resource} is a URL. If not
provided, it is inferred from the \code{resource} value.}

\item{tumorType}{\code{character(1)} specifying the tumor type associated with the
\code{ProvGiga} data. Required if \code{resource} is a local file (file path).}

\item{object}{An object of class \code{ProvGiga}.}

\item{con}{The connection from which data is loaded or to which data is
saved. If this is a \code{character} vector, it is assumed to be a file name and
a corresponding file connection is created and then closed after exporting
the object. If it is a \link[BiocIO:BiocFile-class]{BiocFile} derivative, the
data is loaded from or saved to the underlying resource. If missing, the
function will return the output as a character vector, rather than writing
to a connection.}

\item{format}{The format of the output. If missing and \code{con} is a
file name, the format is derived from the file extension. This argument is
unnecessary when \code{con} is a derivative of \link[BiocIO:BiocFile-class]{BiocFile}.}

\item{text}{If \code{con} is missing, this can be a character vector directly
providing the string data to import.}

\item{...}{Parameters to pass to the format-specific method.}
}
\value{
\itemize{
\item \code{ProvGiga}: An object of class \code{ProvGiga}.
\item \code{import}: A \code{tibble} containing slide-level embeddings along with slide
names and tumor type.
}
}
\description{
The \code{ProvGiga} class represents ProvGiga slide-level CSV files
containing embeddings for histopathology images. It extends the \code{TENxFile}
class from the \code{TENxIO} package, allowing for efficient handling of large
CSV files. The class includes slots to specify the output class when
importing the data, either \code{SpatialExperiment} or
\code{SpatialFeatureExperiment}, the tumor type, and whether the resource is a
URL.

The \code{ProvGiga} constructor function creates an instance of the
\code{ProvGiga} class. The \code{resource} argument can be either a file path or URL
to a ProvGiga CSV file. The \code{tumorType} parameter specifies the tumor
type associated with the ProvGiga data.
}
\details{
The \code{ProvGiga} constructor function can import file paths, URLs, and
\code{TENxFile} objects. If a local file path is provided, the \code{tumorType}
parameter must be specified to indicate the tumor type associated with the
ProvGiga data. If a URL is provided, the tumor type is inferred from the
URL structure.
}
\section{Slots}{

\describe{
\item{\code{tumorType}}{\code{character(1)} specifying the tumor type associated with the
\code{ProvGiga} data.}

\item{\code{level}}{\code{character(1)} specifying the level of ProvGiga data to import.
Must be one of \code{"slide_level"} or \code{"tile_level"}. If not provided, the
level is inferred from the file path or URL.}

\item{\code{is_url}}{\code{logical(1)} indicating whether the resource is a URL.}
}}

\section{\code{show}}{
 The \code{show} method for \code{ProvGiga} objects displays
information about the object, including the resource path and tumor type.
}

\section{\code{import}}{
 The \code{import} method for \code{ProvGiga} objects reads the
ProvGiga CSV file and extracts slide-level embeddings along with the slide
names and tumor type. The embeddings are returned as a \code{tibble} with
columns for slide names, tumor type, and embedding values.
}

\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
## Importing a slide_level ProvGiga CSV file from a local path
slide_prov_url <-
    getCatalog(pipeline = "provgigapath", format = "csv") |>
    dplyr::filter(
        level == "slide_level" &
            filename == paste0(
                "TCGA-OR-A5JJ-01Z-00-DX1.",
                "459B5DFE-47B1-426F-B009-7664C1B6FEEC.csv.gz"
            )
    ) |>
    getFileURLs()

slide_file <- file.path(tempdir(), basename(slide_prov_url))
download.file(slide_prov_url, destfile = slide_file)

ProvGiga(slide_file, level = "slide_level", tumorType = "TCGA_ACC") |>
    import()

## Importing a slide_level ProvGiga CSV file from a URL
ProvGiga(slide_prov_url, tumorType = "TCGA_ACC") |>
    import()

## Import tile_level ProvGiga CSV file from a URL
tile_prov_url <-
    getCatalog(pipeline = "provgigapath", format = "csv") |>
    dplyr::filter(
        level == "tile_level" &
        filename == paste0(
            "TCGA-AA-3556-01Z-00-DX1.",
            "63a74b91-44e8-4ffd-8737-bcf6992183c3.csv.gz"
        )
    ) |>
    getFileURLs()

ProvGiga(tile_prov_url, tumorType = "TCGA_COAD") |>
    import()
\dontshow{\}) # examplesIf}
}
\author{
Ilaria B., Marcel R.
}
