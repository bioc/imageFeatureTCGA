% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/HoverNet.R
\docType{class}
\name{HoverNet}
\alias{HoverNet}
\alias{HoverNet-class}
\alias{HoverNetJSON-class}
\alias{HoverNetH5AD-class}
\alias{HoverNetPNG-class}
\alias{show,HoverNetJSON-method}
\alias{import,HoverNetJSON,ANY,ANY-method}
\alias{import,HoverNetH5AD,ANY,ANY-method}
\alias{.HoverNetPNG}
\alias{import,HoverNetPNG,ANY,ANY-method}
\alias{import,HoverNetParquet,ANY,ANY-method}
\title{Import Hovernet JSON, H5AD, and PNG files into Bioconductor classes}
\usage{
HoverNet(
  resource,
  contours = FALSE,
  outClass = c("SpatialExperiment", "SpatialFeatureExperiment")
)

\S4method{show}{HoverNetJSON}(object)

\S4method{import}{HoverNetJSON,ANY,ANY}(con, format, text, ...)

\S4method{import}{HoverNetH5AD,ANY,ANY}(con, format, text, ...)

\S4method{import}{HoverNetPNG,ANY,ANY}(con, format, text, ...)

\S4method{import}{HoverNetParquet,ANY,ANY}(con, format, text, ...)
}
\arguments{
\item{resource}{\code{character(1)} the file path or URL to the Hovernet JSON
file.}

\item{contours}{\code{logical(1)} whether to include cell contours in the metadata
of the resulting \code{SpatialExperiment} or \code{SpatialFeatureExperiment} object.
Default is \code{FALSE} and used only when importing "JSON" data.}

\item{outClass}{\code{character(1)} specifying the output class when importing
either "JSON" or "H5AD" data into a Bioconductor class object. Must be one
of \code{"SpatialExperiment"} (default) or \code{"SpatialFeatureExperiment"}.}

\item{object}{An object of class \code{HoverNetJSON}.}

\item{con}{The connection from which data is loaded or to which data is
saved. If this is a \code{character} vector, it is assumed to be a file name and
a corresponding file connection is created and then closed after exporting
the object. If it is a \link[BiocIO:BiocFile-class]{BiocFile} derivative, the
data is loaded from or saved to the underlying resource. If missing, the
function will return the output as a character vector, rather than writing
to a connection.}

\item{format}{The format of the output. If missing and \code{con} is a
file name, the format is derived from the file extension. This argument is
unnecessary when \code{con} is a derivative of \link[BiocIO:BiocFile-class]{BiocFile}.}

\item{text}{If \code{con} is missing, this can be a character vector directly
providing the string data to import.}

\item{...}{Parameters to pass to the format-specific method.}
}
\value{
\itemize{
\item \code{HoverNetJSON}: An object of class \code{HoverNetJSON}
\item \verb{import,HoverNetJSON-method}: An object of class \code{SpatialExperiment} or
\code{SpatialFeatureExperiment} containing the cell data and spatial
coordinates extracted from the Hovernet JSON file
\item \verb{import,HoverNetH5AD-method}: An object of class \code{SpatialExperiment} or
\code{SpatialFeatureExperiment} containing the cell data and spatial
coordinates extracted from the Hovernet H5AD file
\item \verb{import,HoverNetPNG-method}: A PNG image as an RGB array as given by
\code{png::readPNG}.
}
}
\description{
The \code{HoverNet} virtual class and its subclasses represent
different file formats used in the HoverNet cell segmentation and
classification pipeline for histopathology images. The \code{HoverNet}
constructor function creates instances of the appropriate subclass based on
the file format of the provided resource. The \code{import} methods for each
subclass read the respective file formats and represent the data as either
a \code{SpatialExperiment} or \code{SpatialFeatureExperiment} object, depending on
the specified output class.

The \code{HoverNetJSON} constructor function creates an instance
of the \code{HoverNetJSON} class. The \code{resource} argument can be either a
file path or URL to a Hovernet JSON file. The \code{contours} parameter
is optional and can be used to include cell contours in the metadata.
The \code{outClass} parameter specifies the output class when importing
the data, either \code{SpatialExperiment} or \code{SpatialFeatureExperiment}.
}
\details{
The \code{HoverNetJSON} class represents Hovernet JSON files used for
cell segmentation and classification in histopathology images. It extends
the \code{TENxFile} class from the \code{TENxIO} package, allowing for efficient
handling of large JSON files. The class includes a slot to indicate whether
cell contours should be included in the metadata when importing the data.
As well as a slot to specify the output class when importing the data,
either \code{SpatialExperiment} or \code{SpatialFeatureExperiment}.
The \code{HoverNetH5AD} class represents Hovernet H5AD files, which contain
similar data but in a different format. The \code{HoverNetPNG} class represents
PNG thumbnail images of the whole-slide images used in HoverNet.

The \code{HoverNetJSON} constructor function can import file paths
and URLs. Remote files are automatically cached using \code{BiocFileCache}
when the \code{import} method is called. This allows for efficient handling
of large JSON files without the need to download them manually.
}
\section{Slots}{

\describe{
\item{\code{contours}}{\code{logical(1)} indicating whether to include cell contours in
the metadata of the resulting \code{SpatialExperiment} or
\code{SpatialFeatureExperiment} object.}

\item{\code{outClass}}{\code{character(1)} specifying the output class when importing the
data. One of \code{"SpatialExperiment"} or \code{"SpatialFeatureExperiment"}.}

\item{\code{is_url}}{\code{logical(1)} indicating whether the resource is a URL.}
}}

\section{\code{show}}{
 The \code{show} method for \code{HoverNetJSON} objects displays the
\code{resource}, \code{contours}, and \code{outClass} slots and vaules.
}

\section{\code{import}}{
 The import method for \code{HoverNetJSON} reads the JSON
file and represents the data as either a \code{SpatialExperiment} or
\code{SpatialFeatureExperiment} object. It extracts cell centroid coordinates,
cell types, and type probabilities, and optionally includes cell contours
in the metadata. The resulting \code{SpatialExperiment} object contains the cell
data in the \code{colData} slot and spatial coordinates in the \code{spatialCoords}
slot of the object.

 The import method for \code{HoverNetH5AD} reads the H5AD file
and represents the data as either a \code{SpatialExperiment} or
\code{SpatialFeatureExperiment} object. It extracts cell centroid coordinates,
cell types, mean intensity, and nearest neighbor distance. The resulting
\code{SpatialExperiment} object contains the cell data in the \code{colData} slot and
spatial coordinates in the \code{spatialCoords} slot of the object.
}

\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
## Manual download and local file input
hov_json_file <-
    getCatalog("hovernet", "json") |>
    dplyr::filter(
        filename == paste0(
            "TCGA-VG-A8LO-01A-01-DX1.",
            "B39A4D64-82A1-4A04-8AB6-918F3058B83B.json.gz"
        )
    ) |>
    getFileURLs()

dest_json <- file.path(tempdir(), basename(hov_json_file))
download.file(hov_json_file, destfile = dest_json)

HoverNet(dest_json, outClass = "SpatialExperiment") |>
    import()

## Direct URL input (with caching)
HoverNet(hov_json_file, outClass = "SpatialExperiment") |>
    import()

## Import as SpatialFeatureExperiment
library(SpatialFeatureExperiment)
HoverNet(dest_json, outClass = "SpatialFeatureExperiment") |>
    import()
\dontshow{\}) # examplesIf}
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
hov_h5ad_file <-
    getCatalog("hovernet", "h5ad") |>
    dplyr::filter(
        filename == paste0(
            "TCGA-VG-A8LO-01A-01-DX1.",
            "B39A4D64-82A1-4A04-8AB6-918F3058B83B.h5ad.gz"
        )
    ) |>
    getFileURLs()
dest_h5ad <- file.path(tempdir(), basename(hov_h5ad_file))
download.file(hov_h5ad_file, destfile = dest_h5ad)

HoverNet(dest_h5ad, outClass = "SpatialExperiment") |>
    import()
\dontshow{\}) # examplesIf}
## Import HoverNetPNG thumbnail from URL
hov_png_url <-
    getCatalog("hovernet", "thumb") |>
    dplyr::filter(
        filename == paste0(
            "TCGA-VG-A8LO-01A-02-DX2.",
            "9B58474C-DAC0-4D45-B13C-0A1EA9E1BC32.png"
        )
    ) |>
    getFileURLs()

HoverNet(hov_png_url) |>
  import()

hov_parq_url <-
    getCatalog("hovernet", "parquet") |>
    dplyr::filter(
        filename == paste0(
            "TCGA-A6-2675-01Z-00-DX1.",
            "d37847d6-c17f-44b9-b90a-84cd1946c8ab.parquet"
        )
    ) |>
    getFileURLs()

HoverNet(hov_parq_url) |>
   import()
}
\author{
Ilaria B., Sehyun O., Marcel R.
}
