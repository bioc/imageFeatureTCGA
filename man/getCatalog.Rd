% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getCatalog.R
\name{getCatalog}
\alias{getCatalog}
\alias{getFileURLs}
\title{Download the catalog of available HoVerNet and ProvGigaPath files}
\usage{
getCatalog(
  pipeline = c("hovernet", "provgigapath"),
  format = c("csv", "thumb", "h5ad", "geojson", "parquet", "json"),
  version = c("1.1.2", "1.1.1", "1.1.0", "1.0.0"),
  redownload = FALSE
)

getFileURLs(catalog)
}
\arguments{
\item{pipeline}{\code{character()} One or both "hovernet" and/or "provgigapath"
specifying which pipeline(s) to include in the catalog. Default includes
both.}

\item{format}{\code{character()} One or more of "csv", "thumb", "h5ad", "geojson",
or "json" specifying which file formats to include in the catalog. Default
includes all.}

\item{version}{\code{character(1L)} The version of the catalog to retrieve. Must
be one of "1.1.1", "1.1.0" or "1.0.0". Default is the latest version,
currently: "1.1.1".}

\item{redownload}{\code{logical(1L)} Whether to redownload the catalog file even
if it is already cached locally. Default is \code{FALSE}.}

\item{catalog}{A \code{tibble} as returned by \code{getCatalog()}.}
}
\value{
\code{getCatalog}: A \code{tibble} containing the full catalog of available
files for the specified pipeline(s).

\code{getFileURLs}: A \code{character()} vector of full URLs for the files
listed in the provided catalog.
}
\description{
The \code{getCatalog} function retrieves a catalog of all available
HoVerNet and ProvGigaPath files, including filenames, sizes, pipelines
used, tumor types, and data levels.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
## Get the full catalog of available files
getCatalog(pipeline = c("hovernet", "provgigapath"), format = "h5ad")
\dontshow{\}) # examplesIf}
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
## Get file URLs from the catalog
getCatalog(pipeline = "hovernet", format = "h5ad") |>
    dplyr::slice(1:10) |>
    getFileURLs()
\dontshow{\}) # examplesIf}
}
